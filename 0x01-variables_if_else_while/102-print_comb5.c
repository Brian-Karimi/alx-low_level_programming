#include <stdio.h>

/**
 *  main - Entry Point
 *  Description: all possible combinations of two two-digit numbers.
 *  numbers should range from 0 to 99
 *  Return: 0
*/
int main(void)
{
	int x;
	int y;

	for(x = "00", x <= "99", x++)
	{
		for(y = x + 1; y <= "99"; y++)
		{
			putchar(x);
			putchar(y);
			p
		}
	}
}
