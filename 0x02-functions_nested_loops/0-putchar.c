#include <main.h>

/**
 * main - Entry Point
 * _putchar- writes the character c to stdout
 * Prgram should print c
 *
 * Return:0
 */
int main(void)
{
	int x = 0;
	char y[] = "_putchar";

	for (x < 9; x++)
	{
		_putchar(y[i]);
	}
	_putchar('\n');
	return (0);
}
