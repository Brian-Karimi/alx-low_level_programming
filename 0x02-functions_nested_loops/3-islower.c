#include "main.h"

/**
 *_islower: checks for lowercase character
_islower: checks for lowercase character.
check if characters are in lowercase, 
 * if lower, return 1
 * else return 0
 * Return 1
*/
int _islower(int c)
{
	if (c >= 97 && c < 123)
	{
		return (1);
	}
	else
	{
		return (0);
}
