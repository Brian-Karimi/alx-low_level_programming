#include "main.h"
/**
*islower: checks if characters is lowercase
* if character is lowercase, the function should return 0 else should return 1
*
* Return: 1
*/
int _islower(int c);
{
	c = 'a';

	if (c >= 97; c < 123)
	{
		return (1);
	}
	else
	{
		return (0);
}
