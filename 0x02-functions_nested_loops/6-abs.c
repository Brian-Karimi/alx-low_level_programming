#include "main.h"

/**
 *_abs- computes the absolute value in  this world
 * print the absolute value of integer input
 *
 * @x: input number as an integer.
*
* Return: absolute value
*/

int _abs(int x)
{
if (x >= 0)
{
	return (x);
}
else
{
	return (x * -1);
}
}
