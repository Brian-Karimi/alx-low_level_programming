#include "main.h"
#include <stdio.h>

/**
 * mul-multiplies two integers
 * @a: integer one
 * @b:integer two
 * Return: Always mul/product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
